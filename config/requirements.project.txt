# Runtime dependencies of the Python pipeline (src/python/).
# These are installed into the Docker image via the Dockerfile.
# Add dependencies here, not in the root requirements.txt (which is local-dev only).

# HTTP + config
requests
python-dotenv

# Data handling
pandas

# Database
SQLAlchemy
psycopg2-binary

# Google Sheets (fetch_google_forms_data.py)
google-api-python-client
google-auth
google-auth-httplib2
